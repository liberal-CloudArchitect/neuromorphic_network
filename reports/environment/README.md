# Environment evidence

This directory stores sanitized environment manifests and platform-specific dependency locks. Manifests must not contain credentials, private channel tokens, or user-specific absolute paths.
