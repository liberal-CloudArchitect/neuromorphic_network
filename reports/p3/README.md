# P3 Reports

P3 分为两层裁决：`GATE-3` 验收预注册实验与复现完整性，`GATE-NN-MVP` 验收人工模块网络的确认性收益。qualification 报告只证明程序路径可运行，不进入正式统计。

正式后台 run 完成前，本目录不包含科学收益结论。大型 checkpoint、逐样本记录和 telemetry 保存在 ignored `artifacts/runs/<run_id>/`；Git 只跟踪冻结配置、checksum、汇总和独立 Gate 报告。
