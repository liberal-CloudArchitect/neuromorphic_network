# P4 报告

本目录保存 P4 可复核的小型摘要；大型 checkpoint、逐样本记录和后台日志保留在被 Git 忽略的 `artifacts/runs/`。

- `qualification.md/json`：CPU 与 MPS 小样本资格证据，只评价工程路径。
- mechanism 与 formal 报告只有对应三 seed 矩阵完成并由独立 verifier 复算后才会创建。

qualification、pilot 或单 seed 结果不得被描述为预测因果、稀疏收益或 network MVP 结论。
