schema_version: p5-mechanism-v1
protocol_version: p5-protocol-v2
profile: qualification
device: cuda
seeds: [7]
train_samples: 64
validation_samples: 32
analysis_samples: 32
test_samples: 32
batch_size: 8
steps_per_task: 2
validation_interval: 2
checkpoint_interval: 2
patience: 10
bootstrap_samples: 200
wall_clock_hours: 2
selected_preset: preset-1
weight_decay: 0.01
dual_budget_weight: 0.001
dual_route_fraction: 0.25
gradient_clip_norm: 1.0
telemetry_enabled: false
output_root: artifacts/runs
control_root: artifacts/p5-cuda/control
qualification_report: artifacts/p5-cuda/qualification-lock.json
pilot_lock: artifacts/p5-cuda/pilot-lock.json
