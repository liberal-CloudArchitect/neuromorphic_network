schema_version: p5-mechanism-v1
protocol_version: p5-protocol-v2
profile: mechanism
device: cuda
seeds: [17, 29, 43]
train_samples: 8192
validation_samples: 2048
analysis_samples: 512
test_samples: 2048
batch_size: 64
steps_per_task: 5000
validation_interval: 100
checkpoint_interval: 100
patience: 10
bootstrap_samples: 10000
wall_clock_hours: 48
selected_preset: preset-1
weight_decay: 0.01
dual_budget_weight: 0.001
dual_route_fraction: 0.25
gradient_clip_norm: 1.0
telemetry_enabled: false
output_root: artifacts/runs
control_root: artifacts/p5-cuda/control
qualification_report: artifacts/p5-cuda/qualification-lock.json
pilot_lock: artifacts/p5-cuda/pilot-lock.json
mechanism_qualification_report: artifacts/p5-cuda/mechanism-qualification-lock.json
